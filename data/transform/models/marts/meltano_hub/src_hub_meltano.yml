config-version: 2
version: 2
sources:
  - name: hub_meltano
    database: "{{ env_var('ATHENA_CATALOG', env_var('SQLFLUFF_ATHENA_CATALOG')) }}"
    # TODO: once the hub meltano instance is migrated here this will be part
    # of this DBT project but for now import it as a source
    schema: "dbt_prod"
    tables:
      - name: fact_repo_metrics