config-version: 2
version: 2
sources:
  - name: tap_spreadsheets_anywhere
    database: '{{ env_var("DBT_SNOWFLAKE_DATABASE_RAW") }}'
    schema: '{{ env_var("DBT_SNOWFLAKE_SOURCE_SCHEMA_PREFIX", "") }}SPREADSHEETS_ANYWHERE'
    tables:
      - name: aws_ips
      - name: gcp_ips
      - name: azure_ips
